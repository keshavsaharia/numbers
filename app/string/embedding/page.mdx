import { stringNavigation } from '@/data/string/navigation'

<TopNavigation id="embedding" group={ stringNavigation } />

# Embeddings

String embeddings are a way to represent strings as vectors in a high-dimensional space. They are used in a variety of applications, including natural language processing, computer vision, and recommendation systems.


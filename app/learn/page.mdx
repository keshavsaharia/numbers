import lessons from '@/data/lessons'
export const metadata = {
    title: lessons.title,
    description: lessons.description
}

# Interactive lessons

These are a series of interactive lessons that explore the fundamentals of binary number systems.

<SectionListBox { ...{...lessons, title: ''} }/>
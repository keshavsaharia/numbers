import InteractiveMNIST from '@/components/ml/cnn/InteractiveMNIST'

# CNN

CNN is a type of neural network that is commonly used in image recognition and classification tasks. 
It is a type of feedforward neural network that is designed to process data in a grid-like structure, such as an image.

<InteractiveMNIST />

This tutorial is organized into the following sections:

- Matrix operations
- Convolutional layers
- Kernels
- Sampling layers
- Training
- Hyperparameters

A tensor can be thought of as an n-dimensional matrix. In the CNN above, tensors will be 3-dimensional with the exception of the output layer.


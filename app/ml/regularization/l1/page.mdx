### L1 Regularization (Lasso)

This promotes **sparsity** by encouraging some weights to become exactly zero, effectively performing **feature selection**.

$$ \Omega(\boldsymbol{\theta}) = \|\boldsymbol{\theta}\|_1 = \sum_{j} |\theta_j| $$

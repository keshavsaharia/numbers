# L2 Regularization (Ridge Regression)

This penalizes large weights and encourages smaller, more evenly distributed values. It keeps all features but shrinks their influence.

$$ \Omega(\boldsymbol{\theta}) = \|\boldsymbol{\theta}\|_2^2 = \sum_{j} \theta_j^2 $$

# Supervised learning

In supervised learning, the model is trained on labeled data—data that includes both the input and the correct output—making it well-suited for tasks such as classification and regression. 